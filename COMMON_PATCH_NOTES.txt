ゲーム名：カタン
共通化バージョン：v1.45
GAME_ID：catan
MAX_PLAYERS：6
既存Worker URL：https://catan-online-server.naitoryo7110.workers.dev
共通管理Worker：https://boardgame-hub-api.naitoryo7110.workers.dev

[CLIENT]

ファイル：index.html
変更箇所：
・タイトルのバージョンを v1.44 へ更新
・プレイヤー名入力上限を共通仕様の32文字へ対応
・HOME／プレイ記録ボタンは追加していない

ファイル：style.css
変更箇所：
・ROOM 1～4をPC 4列へ固定
・スマホ幅では2列×2段
・ROOM状態／参加者名表示用の最小CSSを追加
・ゲーム本編UIは変更していない

ファイル：online.js
変更定数：
・GAME_ID = "catan"
・GAME_NAME = "カタン"
・MAX_PLAYERS = 6
・WORKER_ORIGIN = 既存 window.CATAN_SERVER_URL
・COMMON_MANAGER_URL
・COMMON_PLAYER_NAME_KEY = "boardgamePlayerName"
・ROOM_IDS = room1～room4
・APP_VERSION = "v1.44"

変更関数／追加関数：
・commonSavedName()
・saveCommonNameOnActualStart()
・tokenKey()
・getToken()
・checkRoomJoin()
・roomStatusLabel()
・renderRoomCards()
・resetRoom()
・joinOnlineRoom()
・receiveRoomState()
・sendSettings()
・startOnlineGame()
・leaveOnlineRoom()
・resetOnlineRoom()
・initOnlineApp()
・newActionId()

変更内容：
・共通プレイヤー名はゲーム開始状態受信時だけ保存
・ROOM参加／入力時は boardgamePlayerName へ保存しない
・sessionStorage は入力途中値だけ保持
・ROOMごとの再接続tokenを使用
・空ROOM参加前に /join-check
・既存ROOMはホスト権限確認なし
・ROOM個別初期化は /reset-empty
・共通操作へ actionId を追加
・既存再接続処理を維持してACTIVE_ROOM/ACTIVE_NAMEへ接続
・既存ダイス復旧／ピン同期／ゲーム本編通信は維持

[WORKER]

ファイル：src/index.js
追加定数：
・GAME_ID
・GAME_NAME
・MAX_PLAYERS
・DEFAULT_COMMON_MANAGER_URL

追加共通関数：
・commonBase()
・parseJsonSafe()
・checkHostPermission()
・writePlayRecord()
・createPlayRecordForStart()
・isDuplicateAction()
・cleanToken()

変更関数：
・freshRoom()
・CatanRoom.ensureState()
・CatanRoom.fetch()
・CatanRoom.webSocketMessage()
・resetRoomCompletely() は既存処理を利用

変更内容：
・GET /join-check を追加
・POST /reset-empty を追加
・WebSocket参加時にも空ROOMだけホスト権限を再確認
・権限なし文言を「ホスト権限がありません」に統一
・共通管理Worker接続失敗時は「ホスト権限を確認できません。」
・既存ROOMへの一般参加はホスト確認を通さない
・ゲーム開始正式成立後だけ /api/play/start を1回試行
・recordEpoch / gameSequence / recordId / gameSessionId を使用
・unknown時はプレイ記録を自動再送しない
・processedActions により共通操作の二重送信を抑止
・空ROOM個別初期化は接続中人間がいる場合409
・完全初期化で記録状態／processedActionsも初期化

ファイル：wrangler.jsonc
変更内容：
・既存設定を維持
・vars.COMMON_MANAGER_URL を追加
・Worker名は catan-online-server のまま

ファイル：0_CHECK_SERVER.bat
・ASCIIのみ
・BOMなし
・CRLF
・npm/npxはcall経由
・成功時／失敗時pause

ファイル：1_DEPLOY_SERVER.bat
・ASCIIのみ
・BOMなし
・CRLF
・npm/npxはcall経由
・成功時／失敗時pause

[変更していない主要部分]

・game.js のゲームルール
・CPUロジック
・カタン本編の進行
・既存ダイス進行不能対策
・盤面ピン
・漁師
・1920×1080調整
・既存ゲームWorker URL

[次回更新]

最初にこの COMMON_PATCH_NOTES.txt を確認し、
online.js / style.css / index.html / Worker src/index.js / wrangler.jsonc
の上記接続点だけを確認してください。
ゲーム本編全体の再解析は不要です。


[CPU AI PATCH v1.45]

CLIENT game.js
変更関数／追加関数：
・cpuDiscardHalf()
・scheduleCpuDiscard()
・resumeCpuDiscardIfNeeded()
・cpuVertexStrategicScore()
・cpuBestSettlementTarget()
・cpuBestCityTarget()
・cpuStrategicGoals()
・cpuChooseGoal()
・cpuResourceKeepValue()
・cpuChooseDiscardResource()
・cpuVictimScore()
・cpuChooseVictimFromCandidates()
・cpuBestRobberHexId()
・robberTargetScore()
・cpuMoveRobber()
・cpuPlayYearOfPlenty()
・cpuPlayMonopoly()
・cpuPlayRoadBuilding()
・cpuUseStrategicDevelopment()
・cpuBuildPhase()
・cpuTryBankTrade()
・futureVertexScore()
・setupVertexScore()
・roadExpansionScore()
・cpuBestRoadTarget()
・cpuUseFish()

変更内容：
・CPU初期配置を資源バランス／出目／港／漁場基準へ改善
・CPU建設優先順位を状況評価方式へ変更
・CPU銀行／港交易を目標建設基準へ変更
・CPU発展カードを状況に応じて使用
・盗賊配置／強奪相手の優先順位を改善
・CPUの7資源半減を戦略的かつ必ず実行
・CPU破棄フェーズを独立タイマー化し、再同期後も復旧可能
・漁師CPU判断を改善

CLIENT online.js
変更関数：
・scheduleCpuIfNeeded()

変更内容：
・現在手番が人間でも、7のCPU破棄待ちならCPU破棄処理を再開
・共通オンラインv4のROOM／再接続／ホスト／記録処理は変更なし

WORKER
・変更なし

ゲームルール：
・変更なし
・1ターン1回の通常建設制限など既存ルールを維持
