カタン：共通オンライン規格 移植メモ
共通化バージョン：v1.43
適用規格：オンラインボドゲ共通化_実装規格_v4

[ADAPTER]
GAME_ID = catan
GAME_NAME = カタン
MAX_PLAYERS = 6
WORKER_ORIGIN = https://catan-online-server.naitoryo7110.workers.dev
APP_VERSION = v1.43

タイトル描画箇所 = github-pages/index.html / showLobbyScreen()
ROOM一覧描画関数 = github-pages/online.js / renderRoomCards()
ROOM参加関数 = github-pages/online.js / joinOnlineRoom()
ゲーム開始関数 = github-pages/online.js / startOnlineGame()
再接続関数 = github-pages/online.js / scheduleReconnect(), joinOnlineRoom(...,{reconnect:true})
ROOM初期化関数 = github-pages/online.js / manualResetRoomFromTitle(), resetOnlineRoom()
WorkerのROOMクラス/処理 = cloudflare-worker/src/index.js / CatanRoom
Workerの保存関数 = cloudflare-worker/src/index.js / CatanRoom.save()
参加者配列 = CatanRoom.state.members
CPU配列 = ゲーム開始後の state.game.players 内 human=false のプレイヤー
ゲーム状態 = CatanRoom.state.game / CatanRoom.state.phase

[CLIENT]
ファイル：index.html
変更内容：
・表示バージョンを v1.43 へ更新
・名前入力 maxlength を32へ変更
・HOME／プレイ記録ボタンは追加していない

ファイル：style.css
変更内容：
・ROOM一覧はPC 4列を維持
・スマホは430px以下を含め2列×2段へ統一
・ROOMカードの参加者名表示用スタイルを追加

ファイル：online.js
変更関数／箇所：
・共通定数
  GAME_ID / GAME_NAME / MAX_PLAYERS / WORKER_ORIGIN / COMMON_MANAGER_URL /
  COMMON_PLAYER_NAME_KEY / ROOM_IDS / APP_VERSION を規格に合わせて定義
・commonSavedName()
  boardgamePlayerName を初期表示候補として読む
・saveCommonNameOnActualStart()
  boardgamePlayerName は実ゲーム開始状態受信時だけ保存
・tokenKey(), getToken()
  ROOMごとの再接続tokenを使用
・checkRoomJoin()
  WebSocket接続前に /join-check を実行
・renderRoomCards()
  固定 ROOM 1～4、現在人数/6人、参加者名、待機中/ゲーム中、参加/再接続、個別初期化を表示
・manualResetRoomFromTitle()
  /reset-empty で指定ROOMだけ初期化
・joinOnlineRoom()
  空ROOMホスト確認後に接続。既存ROOMは通常参加。再接続tokenをclientIdとして継続利用
・scheduleReconnect()
  通信断、ページ復帰、online復帰時の再接続を既存方式へ接続
・receiveRoomState()
  gameSessionId付きplaying状態を受信した時だけ共通名保存
・sendSettings(), startOnlineGame(), resetOnlineRoom()
  actionIdを付与して共通化操作の二重送信を防止
・initOnlineApp()
  sessionStorage入力途中値 → 再接続用active name → boardgamePlayerName の順で名前を復元
  visibilitychange / online 復帰時に再接続

変更していないもの：
・game.js のゲームルール、CPUロジック、手番処理、盤面処理
・既存Cloudflare Workers URL
・ゲーム本編UI

[WORKER]
ファイル：src/index.js
変更関数／箇所：
・共通定数
  GAME_ID=catan / GAME_NAME=カタン / MAX_PLAYERS=6 /
  DEFAULT_COMMON_MANAGER_URL を追加
・commonBase(), parseJsonSafe(), checkHostPermission()
  共通管理Workerの /api/host/check を使用
・writePlayRecord()
  共通管理Workerの /api/play/start へ開始時だけ送信。通信結果unknown時も自動再送しない
・freshRoom(), ensureState()
  recordEpoch / gameSequence / recordAttempt / gameSessionId / startedAt /
  playRecordStatus / processedActions を追加・既存ROOM状態へ補完
・isDuplicateAction()
  playerId + actionId で共通化操作の二重処理を防止
・/join-check
  既存tokenまたは1人以上いるROOMではホスト確認なし
  完全な空ROOMだけ checkHostPermission() を実行
・WebSocket新規参加
  クライアント事前確認だけに依存せず、空ROOM時はWorker側でも再度ホスト確認
  権限なし文言は「ホスト権限がありません」
  権限確認失敗時だけ「ホスト権限を確認できません。」
・/reset-empty
  接続中の人間がいるROOMは409で拒否
  指定ROOMだけ freshRoom() へ完全初期化
・summary
  ROOMカード表示用の参加者名を返す
・createPlayRecordForStart()
  正式な start_game 成立時だけ recordId/gameSessionId を生成して1件送信
・start_game
  actionId重複防止後、playing確定→開始記録→状態配信
・set_settings / reset_room
  actionId重複防止を追加
・WebSocket再接続
  connectionIdを追加し、旧Socketのcloseが新接続を切断扱いにしないよう修正

ファイル：wrangler.jsonc
変更内容：
・既存設定を維持したまま COMMON_MANAGER_URL を vars に追加

ファイル：0_CHECK_SERVER.bat
変更内容：
・規格に合わせ新規追加
・ASCIIのみ / BOMなし / CRLF / call経由 / 成否ともpause

ファイル：1_DEPLOY_SERVER.bat
変更内容：
・ASCIIのみ / BOMなし / CRLF / npm,npxはcall経由 / 成否ともpause

[IMPORTANT]
・既存Worker URL： https://catan-online-server.naitoryo7110.workers.dev を変更していない
・HOME／プレイ記録ボタンはタイトルにもゲーム中にも表示しない
・boardgamePlayerName はROOM参加、入力、ページ表示では更新しない
・プレイ記録はROOM参加、更新、再接続では送信しない
・同じROOMで次ゲーム開始時は gameSequence を増加
・ROOM完全初期化時は recordEpoch を再生成し記録状態と processedActions を初期化

[CHECKED]
・online.js JavaScript構文確認：OK
・Worker index.js 構文確認：OK
・Worker単体テスト：OK
  - 空ROOM＋権限あり join-check 成功
  - 空ROOM＋権限なし 403「ホスト権限がありません」
  - 既存ROOM＋権限なし host checkなしで成功
  - 接続中ROOMの reset-empty 拒否
  - 空ROOMの reset-empty 成功
  - start_game 時のみプレイ記録1件生成
・既存Worker URL維持：OK
・固定ROOM 1～4：OK
・PC 4列 CSS：OK
・スマホ 2列×2段 CSS：OK
・boardgamePlayerName 書込み箇所が開始状態受信時だけ：OK
・プレイ記録呼出し箇所が start_game だけ：OK
・再接続token / active ROOM / visibility復帰処理：OK
